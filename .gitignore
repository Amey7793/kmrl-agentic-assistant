# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Source PDFs - 363MB total, one file (KMRL_AR_2015-16_EN.pdf, 144MB) exceeds
# GitHub's 100MB/file limit outright. Only needed to rebuild the KB index from
# scratch via index_documents.py - not needed to run the app.
Knowledge Base/PDFs/

# Unused legacy artifact - not referenced anywhere in the codebase
kmrl_agent/kb_index.pkl

# NOTE: gtfs.db, lost_found.db, bm25_index.pkl, processed_images.json, and
# chroma_db/ are intentionally committed (not ignored) - they're small
# (~37MB total) and collaborators need them to run the app without first
# installing Ollama and re-running the full ingestion pipeline.

# Streamlit
.streamlit/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
